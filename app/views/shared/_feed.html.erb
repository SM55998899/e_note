<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
  class: "my-pagination-class",
  params: {
    controller: :static_pages,
    action: :diary
  } %>

<% end %>
